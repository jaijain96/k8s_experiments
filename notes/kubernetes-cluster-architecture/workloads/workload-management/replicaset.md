# [`ReplicaSet`](https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/)

A `ReplicaSet`'s purpose is to maintain a stable set of replica `Pod`s running
at any given time.

## How a ReplicaSet works

